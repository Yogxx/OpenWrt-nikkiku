name: testing-nikki-packages

on:
  push:
    tags:
      - v*
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag release"
        required: true
        default: "v0.0.0"
      upload_telegram:
        description: "Upload ke Telegram?"
        type: boolean
        default: false

env:
  UPLOAD_TELEGRAM: ${{ github.event.inputs.upload_telegram || 'false' }}
  RELEASE_TAG: ${{ github.event.inputs.tag || github.ref_name }}

jobs:
  release:
    name: ${{ matrix.arch }}-${{ matrix.branch }} release
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        arch:
          - aarch64_generic
          - aarch64_cortex-a72
          - x86_64
        branch:
          - openwrt-24.10
          - openwrt-25.12

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build packages
        uses: openwrt/gh-action-sdk@main
        env:
          ARCH: ${{ matrix.arch }}-${{ matrix.branch }}
          FEEDNAME: nikki
          PACKAGES: luci-app-nikki
          INDEX: 1
          FEEDS: "routing telephony"
          KEY_BUILD: ${{ secrets.KEY_BUILD }}
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          NO_REFRESH_CHECK: true

      - name: Compress output
        run: |
          tar -czf nikki_${{ matrix.arch }}-${{ matrix.branch }}.tar.gz \
            -C bin/packages/${{ matrix.arch }}/nikki .

      - name: Upload Telegram
        if: env.UPLOAD_TELEGRAM == 'true'
        run: |
          curl -s -F document=@nikki_${{ matrix.arch }}-${{ matrix.branch }}.tar.gz \
            -F chat_id=${{ secrets.TG_CHAT_ID }} \
            -F caption="📦 Nikki ${RELEASE_TAG} | ${{ matrix.arch }} | ${{ matrix.branch }}" \
            https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendDocument
